services:
  app:
    build: .
    image: linkedin-scraper:latest
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
    volumes:
      - ./data:/app/data
    shm_size: "1gb"
