Flask>=3.0
# Pinned so the Chromium build downloaded at Docker build time
# (`playwright install chromium`) always matches the client library.
playwright==1.48.0
pandas>=2.0
openpyxl>=3.1
python-dotenv>=1.0
gunicorn>=22.0
