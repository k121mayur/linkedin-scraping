Flask>=3.0
# Pinned to match the Playwright Docker base image (v1.48.0) so the preinstalled
# browser binaries always match the client. Bumping this requires bumping the
# base image tag in the Dockerfile in lockstep, or the browser won't launch.
playwright==1.48.0
pandas>=2.0
openpyxl>=3.1
python-dotenv>=1.0
gunicorn>=22.0
